identity:
  name: thread_get_user_context
  author: obadakhalili
  label:
    en_US: Thread Get User Context
    zh_Hans: 获取线程用户上下文

description:
  human:
    en_US: Get thread memory context via client.thread.get_user_context(thread_id)
    zh_Hans: 通过 client.thread.get_user_context(thread_id) 获取线程记忆上下文
  llm: Get thread memory context via client.thread.get_user_context(thread_id)

parameters:
  - name: thread_id
    type: string
    required: true
    label:
      en_US: Thread ID
      zh_Hans: 线程 ID
    human_description:
      en_US: Thread ID
      zh_Hans: 线程 ID
    llm_description: Thread ID
    form: llm
  - name: template_id
    type: string
    required: false
    label:
      en_US: Template ID
      zh_Hans: 模板 ID
    human_description:
      en_US: Optional template_id
      zh_Hans: 可选 template_id
    llm_description: Optional template_id
    form: llm

extra:
  python:
    source: tools/thread_get_user_context/main.py
