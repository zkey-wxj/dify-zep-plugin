identity:
  name: user_update
  author: wxj
  label:
    en_US: User Update
    zh_Hans: 更新用户

description:
  human:
    en_US: Update user info via client.user.update(user_id)
    zh_Hans: 通过 client.user.update(user_id) 更新用户信息
  llm: Update user info via client.user.update(user_id)

parameters:
  - name: user_id
    type: string
    required: true
    label:
      en_US: User ID
      zh_Hans: 用户 ID
    human_description:
      en_US: User ID
      zh_Hans: 用户 ID
    llm_description: User ID
    form: llm
  - name: email
    type: string
    required: false
    label:
      en_US: Email
      zh_Hans: 邮箱
    human_description:
      en_US: Optional email
      zh_Hans: 可选邮箱
    llm_description: Optional email
    form: llm
  - name: first_name
    type: string
    required: false
    label:
      en_US: First Name
      zh_Hans: 名
    human_description:
      en_US: Optional first name
      zh_Hans: 可选名
    llm_description: Optional first name
    form: llm
  - name: last_name
    type: string
    required: false
    label:
      en_US: Last Name
      zh_Hans: 姓
    human_description:
      en_US: Optional last name
      zh_Hans: 可选姓
    llm_description: Optional last name
    form: llm
  - name: metadata_json
    type: string
    required: false
    label:
      en_US: Metadata JSON
      zh_Hans: 元数据 JSON
    human_description:
      en_US: Optional JSON object metadata
      zh_Hans: 可选 JSON 对象元数据
    llm_description: Optional JSON object metadata
    form: llm

extra:
  python:
    source: tools/user_update/main.py
