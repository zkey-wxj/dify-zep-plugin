identity:
  name: graph_node_get
  author: wxj
  label:
    en_US: Graph Node Get
    zh_Hans: 获取图节点

description:
  human:
    en_US: Get detailed information for a specific graph node by UUID.
    zh_Hans: 根据 UUID 获取单个图节点的详细信息。
  llm: Get detailed information for a specific graph node by UUID.

parameters:
  - name: uuid
    type: string
    required: true
    label:
      en_US: UUID
      zh_Hans: UUID
    human_description:
      en_US: Node UUID
      zh_Hans: 节点 UUID
    llm_description: Node UUID
    form: llm

extra:
  python:
    source: tools/graph_node_get/main.py
