identity:
  name: graph_episode_get
  author: wxj
  label:
    en_US: Graph Episode Get
    zh_Hans: 获取 Episode

description:
  human:
    en_US: Get the source episode record by UUID.
    zh_Hans: 根据 UUID 获取原始 Episode 记录。
  llm: Get the source episode record by UUID.

parameters:
  - name: uuid
    type: string
    required: true
    label:
      en_US: UUID
      zh_Hans: UUID
    human_description:
      en_US: Episode UUID
      zh_Hans: Episode UUID
    llm_description: Episode UUID
    form: llm

extra:
  python:
    source: tools/graph_episode_get/main.py
