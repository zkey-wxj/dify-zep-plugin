identity:
  name: thread_delete
  author: wxj
  label:
    en_US: Thread Delete
    zh_Hans: 删除线程

description:
  human:
    en_US: Delete thread via client.thread.delete(thread_id)
    zh_Hans: 通过 client.thread.delete(thread_id) 删除线程
  llm: Delete thread via client.thread.delete(thread_id)

parameters:
  - name: thread_id
    type: string
    required: true
    label:
      en_US: Thread ID
      zh_Hans: 线程 ID
    human_description:
      en_US: Thread ID
      zh_Hans: 线程 ID
    llm_description: Thread ID
    form: llm

extra:
  python:
    source: tools/thread_delete/main.py
