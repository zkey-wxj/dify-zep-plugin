identity:
  name: graph_edge_get_by_graph_id
  author: wxj
  label:
    en_US: Graph Edge Get By Graph ID
    zh_Hans: 按图谱 ID 获取边

description:
  human:
    en_US: List relationship edges from a standalone graph.
    zh_Hans: 获取独立图谱中的关系边列表。
  llm: List relationship edges from a standalone graph.

parameters:
  - name: graph_id
    type: string
    required: true
    label:
      en_US: Graph ID
      zh_Hans: 图谱 ID
    human_description:
      en_US: Graph ID
      zh_Hans: 图谱 ID
    llm_description: Graph ID
    form: llm
extra:
  python:
    source: tools/graph_edge_get_by_graph_id/main.py
