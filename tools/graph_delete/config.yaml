identity:
  name: graph_delete
  author: wxj
  label:
    en_US: Graph Delete
    zh_Hans: 删除图谱

description:
  human:
    en_US: Delete a standalone graph and all knowledge stored in it.
    zh_Hans: 删除指定独立图谱及其全部知识数据。
  llm: Delete a standalone graph and all knowledge stored in it.

parameters:
  - name: graph_id
    type: string
    required: true
    label:
      en_US: Graph ID
      zh_Hans: 图谱 ID
    human_description:
      en_US: Graph ID
      zh_Hans: 图谱 ID
    llm_description: Graph ID
    form: llm

extra:
  python:
    source: tools/graph_delete/main.py
