identity:
  name: graph_observation_get
  author: wxj
  label:
    en_US: Graph Observation Get
    zh_Hans: 获取 Observation

description:
  human:
    en_US: Get a derived observation record by UUID.
    zh_Hans: 根据 UUID 获取一条 Observation 记录。
  llm: Get a derived observation record by UUID.

parameters:
  - name: uuid
    type: string
    required: true
    label:
      en_US: UUID
      zh_Hans: UUID
    human_description:
      en_US: Fact UUID
      zh_Hans: Fact UUID
    llm_description: Fact UUID
    form: llm

extra:
  python:
    source: tools/graph_observation_get/main.py
