identity:
  name: graph_observation_delete
  author: wxj
  label:
    en_US: Graph Observation Delete
    zh_Hans: 删除 Observation

description:
  human:
    en_US: Delete an observation by UUID (not available in current plugin runtime).
    zh_Hans: 根据 UUID 删除 Observation（当前插件运行环境暂不可用）。
  llm: Delete an observation by UUID (not available in current plugin runtime).

parameters:
  - name: uuid
    type: string
    required: true
    label:
      en_US: UUID
      zh_Hans: UUID
    human_description:
      en_US: Fact UUID
      zh_Hans: Fact UUID
    llm_description: Fact UUID
    form: llm

extra:
  python:
    source: tools/graph_observation_delete/main.py
